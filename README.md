<h1>📝 Smart Blog Web Setup</h1>

<p><strong>Repository link:</strong> <a href="https://github.com/shamodhas-ijse-teaching/smart-blog-web-72" target="_blank">Smart Blog Web</a></p>

<h2>📂 Project Structure</h2>

<pre>
smart-blog-web-72/
├── frontend/    # React/Vite Frontend
└── backend/     # Node/Express Backend
</pre>

<h2>⚡ Frontend (FE) Setup</h2>
<ol>
  <li>Open terminal in <code>frontend</code> folder.</li>
  <li>If <code>node_modules</code> folder is missing, run:
    <pre>npm install</pre>
  </li>
  <li>Optional: open VS Code:
    <pre>code .</pre>
  </li>
  <li>Start development server:
    <pre>npm run dev</pre>
  </li>
</ol>

<h2>⚡ Backend (BE) Setup</h2>
<ol>
  <li>Open terminal in <code>backend</code> folder.</li>
  <li>If <code>node_modules</code> folder is missing, run:
    <pre>npm install</pre>
  </li>
  <li>Optional: open VS Code:
    <pre>code .</pre>
  </li>
  <li>Start backend server:
    <pre>npm run dev</pre>
  </li>
</ol>

<blockquote>
⚠️ <strong>Note:</strong><br>
- Run Frontend and Backend in separate terminals.<br>
- Make sure Node.js ≥ 18 is installed.<br>
- If backend uses <code>.env</code>, copy <code>.env.example</code> → <code>.env</code> and fill in required values.
</blockquote>
